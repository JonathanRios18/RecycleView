<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <ImageView
        android:id="@+id/imageViewNinja"
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_marginEnd="16dp"
        android:scaleType="centerCrop"
        android:contentDescription="Ninja Image"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

    <TextView
        android:id="@+id/textViewName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:textStyle="bold"
        android:textSize="18sp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toEndOf="@id/imageViewNinja"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="Nombre" />

    <TextView
        android:id="@+id/textViewWeapon"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/textViewName"
        app:layout_constraintStart_toEndOf="@id/imageViewNinja"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="Arma" />

    <TextView
        android:id="@+id/textViewFeature"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/textViewWeapon"
        app:layout_constraintStart_toEndOf="@id/imageViewNinja"
        app:layout_constraintEnd_toEndOf="parent"
        android:text="Característica" />

</androidx.constraintlayout.widget.ConstraintLayout>
